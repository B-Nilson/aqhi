source("renv/activate.R")

if (interactive()) {
  suppressMessages(require(devtools))
}

options(
  "Authors@R" = utils::person(
    "Brayden", "Nilson",
    email = "brayden.nilson@ec.gc.ca",
    role = c("aut", "cre")
  ),
  License = "MIT + file LICENSE"
)
